apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-node-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-node
  template:
    metadata:
      labels:
        app: redis-node
    spec:
      containers:
      - name: redis-node
        image: redis-node-img
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: redis-node-service
spec:
  selector:
    app: redis-node
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
